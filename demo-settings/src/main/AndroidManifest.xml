<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aliyun.playerkit.example.settings">

    <!--  The application needs to access the device's camera  -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application>
        <activity
            android:name="com.aliyun.playerkit.example.settings.SettingActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.Light">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="settings"
                    android:scheme="playerkit" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.aliyun.playerkit.example.settings.link.LinkActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.Light">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="link"
                    android:scheme="playerkit" />
            </intent-filter>
        </activity>
    </application>

</manifest>